name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: cd build && cmake ..
    - name: make
      run: cmake --build . --parallel 2
    - name: run tests
      run: ctest
